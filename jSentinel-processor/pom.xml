<?xml version="1.0" encoding="UTF-8"?>
<!--

    Copyright © 2018 Sven Ruppert (sven.ruppert@gmail.com)

    Licensed under the EUPL, Version 1.2 or - as soon they will be
    approved by the European Commission - subsequent versions of the
    EUPL (the "Licence"); You may not use this work except in
    compliance with the Licence. You may obtain a copy of the Licence at:

    https://joinup.ec.europa.eu/software/page/eupl

-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <parent>
    <groupId>com.svenruppert.jsentinel</groupId>
    <artifactId>jSentinel-parent</artifactId>
    <version>00.79.20</version>
  </parent>
  <modelVersion>4.0.0</modelVersion>

  <artifactId>jSentinel-processor</artifactId>
  <name>jSentinel Annotation Processor</name>
  <description>
    Compile-time annotation processor that generates a Secured wrapper
    subclass for each @Secured-annotated concrete class. The generated
    wrapper routes every public, non-final, non-static method through
    JSentinelEnforcer when the method (or class) carries a method-security
    annotation (@RequiresRole, @RequiresPermission, @RequiresAnyPermission,
    @RequiresAllPermissions, @RequiresPolicy).

    Runtime sibling: SecuredProxy in jSentinel-standalone for the
    JDK-dynamic-proxy path on interfaces.
  </description>

  <properties>
    <proxybuilder.version>00.11.00</proxybuilder.version>
    <compile-testing.version>0.21.0</compile-testing.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>com.svenruppert.jsentinel</groupId>
      <artifactId>jSentinel-core</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>com.svenruppert</groupId>
      <artifactId>proxybuilder</artifactId>
      <version>${proxybuilder.version}</version>
    </dependency>
    <!--
      The annotation module ships the @GeneratedByProxyBuilder marker
      that proxybuilder emits into every generated wrapper class.
      Consumers of jSentinel-processor (via <annotationProcessorPaths>)
      pull this dependency through their own compile-classpath; the
      processor needs it at processing time so JavaPoet can resolve
      the annotation type.
    -->
    <dependency>
      <groupId>com.svenruppert</groupId>
      <artifactId>proxybuilder-annotations</artifactId>
      <version>${proxybuilder.version}</version>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.google.testing.compile</groupId>
      <artifactId>compile-testing</artifactId>
      <version>${compile-testing.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!--
        We are building an annotation processor. Don't let javac trigger
        annotation processing against this module's own sources (would
        try to run the processor that we are still compiling).
      -->
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <proc>none</proc>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>license-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>first</id>
            <phase>none</phase>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>