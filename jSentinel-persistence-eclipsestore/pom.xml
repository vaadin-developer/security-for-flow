<?xml version="1.0" encoding="UTF-8"?>
<!--

    Copyright © 2018 Sven Ruppert (sven.ruppert@gmail.com)

    Licensed under the EUPL, Version 1.2 or - as soon they will be
    approved by the European Commission - subsequent versions of the
    EUPL (the "Licence"); You may not use this work except in
    compliance with the Licence. You may obtain a copy of the Licence at:

    https://joinup.ec.europa.eu/software/page/eupl

-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <parent>
    <groupId>com.svenruppert.jsentinel</groupId>
    <artifactId>jSentinel-parent</artifactId>
    <version>00.75.20</version>
  </parent>
  <modelVersion>4.0.0</modelVersion>

  <artifactId>jSentinel-persistence-eclipsestore</artifactId>
  <name>jSentinel Persistence — Eclipse Store</name>
  <description>
    Eclipse-Store-backed implementations of every Phase-2 persistence
    store (AuditEvent, Session, LoginAttempt, RoleAssignment,
    BootstrapState, RememberMe / PasswordReset / EmailVerification
    tokens, ApiKey, RefreshToken, RateLimit). Each implementation is
    verified bit-for-bit against the contract shipped by
    jSentinel-persistence-testkit. The runtime root object
    (EclipseStoreJSentinelRoot) is package-private — applications
    consume the stores through their existing Phase-2 SPIs and
    control the lifecycle through the public
    EclipseStoreJSentinelStorage facade.
  </description>

  <properties>
    <eclipse-store.version>4.1.0</eclipse-store.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>com.svenruppert.jsentinel</groupId>
      <artifactId>jSentinel-core</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.eclipse.store</groupId>
      <artifactId>storage-embedded</artifactId>
      <version>${eclipse-store.version}</version>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.svenruppert.jsentinel</groupId>
      <artifactId>jSentinel-persistence-testkit</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>license-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>first</id>
            <phase>none</phase>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>