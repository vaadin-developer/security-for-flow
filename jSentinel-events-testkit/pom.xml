<?xml version="1.0" encoding="UTF-8"?>
<!--

    Copyright © 2018 Sven Ruppert (sven.ruppert@gmail.com)

    Licensed under the EUPL, Version 1.2 or - as soon they will be
    approved by the European Commission - subsequent versions of the
    EUPL (the "Licence"); You may not use this work except in
    compliance with the Licence. You may obtain a copy of the Licence at:

    https://joinup.ec.europa.eu/software/page/eupl

-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <parent>
    <groupId>com.svenruppert.jsentinel</groupId>
    <artifactId>jSentinel-parent</artifactId>
    <version>00.75.10-SNAPSHOT</version>
  </parent>
  <modelVersion>4.0.0</modelVersion>

  <artifactId>jSentinel-events-testkit</artifactId>
  <name>jSentinel Events — Contract testkit</name>
  <description>
    V00.75 contract test suites (JUnit-5 @Test default interfaces) for
    the seven Security Event Bus SPIs: PayloadCodec, SignatureAlgorithm,
    KeyManagement, ReplayStore, SequenceStore, ProducerPolicy,
    EventStore. Persistence-tech-agnostic — every adapter
    implementation hands a fresh SPI instance into a newStore() method
    and inherits the full contract.
  </description>

  <dependencies>
    <dependency>
      <groupId>com.svenruppert.jsentinel</groupId>
      <artifactId>jSentinel-events</artifactId>
      <version>${project.version}</version>
    </dependency>

    <!-- Contract suites use @Test default methods, so the API is compile-scoped. -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-api</artifactId>
      <scope>compile</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
