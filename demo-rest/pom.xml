<?xml version="1.0" encoding="UTF-8"?>
<!--

    Copyright © 2018 Sven Ruppert (sven.ruppert@gmail.com)

    Licensed under the EUPL, Version 1.2 or - as soon they will be
    approved by the European Commission - subsequent versions of the
    EUPL (the "Licence"); You may not use this work except in
    compliance with the Licence. You may obtain a copy of the Licence at:

    https://joinup.ec.europa.eu/software/page/eupl

-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <parent>
    <groupId>com.svenruppert.jsentinel</groupId>
    <artifactId>jSentinel-parent</artifactId>
    <version>00.74.20</version>
  </parent>
  <modelVersion>4.0.0</modelVersion>

  <artifactId>demo-rest</artifactId>
  <name>jSentinel - REST Demo</name>
  <description>Demo application for REST security, permissions, protected handlers, and HTTP status mapping.</description>

  <properties>
    <!--
      demo-rest is consumed as a test-scope dependency by
      demo-vaadin-rest-client (HttpDemoBackendClientTest spins up an
      in-process DemoRestServer). It must therefore land in the local
      ~/.m2 — install must NOT be skipped. Remote deploy stays off.
    -->
    <maven.deploy.skip>true</maven.deploy.skip>
    <gpg.skip>true</gpg.skip>
  </properties>

  <dependencies>
    <dependency>
      <groupId>com.svenruppert.jsentinel</groupId>
      <artifactId>jSentinel-core</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>com.svenruppert.jsentinel</groupId>
      <artifactId>jSentinel-rest</artifactId>
      <version>${project.version}</version>
    </dependency>
    <!--
      V00.72 fluent bootstrap. DemoRestServer.main() calls
      RestSecurity.bootstrap() and prints the JSentinelRuntime.log()
      banner before the server starts serving requests.
    -->
    <dependency>
      <groupId>com.svenruppert.jsentinel</groupId>
      <artifactId>jSentinel-dx</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>com.svenruppert.jsentinel</groupId>
      <artifactId>jSentinel-dx-rest</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>com.svenruppert.jsentinel</groupId>
      <artifactId>demo-rest-shared</artifactId>
      <version>${project.version}</version>
    </dependency>
    <!--
      com.svenruppert:core supplies HasLogger and HttpStatus — used by
      DemoHandlers (and the demo tests) to wire status-code constants
      and structured logging without rolling our own enum. Version is
      managed by the dependencies parent.
    -->
    <dependency>
      <groupId>com.svenruppert</groupId>
      <artifactId>core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!-- deploy is suppressed via the maven.deploy.skip property above. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-deploy-plugin</artifactId>
        <configuration>
          <skip>true</skip>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>license-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>first</id>
            <phase>none</phase>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.5.0</version>
        <configuration>
          <mainClass>com.svenruppert.jsentinel.demo.rest.server.DemoRestServer</mainClass>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>