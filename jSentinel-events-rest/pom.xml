<?xml version="1.0" encoding="UTF-8"?>
<!--

    Copyright © 2018 Sven Ruppert (sven.ruppert@gmail.com)

    Licensed under the EUPL, Version 1.2 or - as soon they will be
    approved by the European Commission - subsequent versions of the
    EUPL (the "Licence"); You may not use this work except in
    compliance with the Licence. You may obtain a copy of the Licence at:

    https://joinup.ec.europa.eu/software/page/eupl

-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <parent>
    <groupId>com.svenruppert.jsentinel</groupId>
    <artifactId>jSentinel-parent</artifactId>
    <version>00.75.10-SNAPSHOT</version>
  </parent>
  <modelVersion>4.0.0</modelVersion>

  <artifactId>jSentinel-events-rest</artifactId>
  <name>jSentinel Events — REST / SSE bridge</name>
  <description>
    V00.75 REST/SSE bridge over the Security Event Bus core. Provides
    GET /api/events/stream (server-sent events with Last-Event-ID
    resume) and POST /api/events for external producers that submit
    signed envelopes. Reuses jSentinel-rest's authorization filter;
    the Consume-Pipeline from jSentinel-events runs end-to-end on
    every accepted envelope.
  </description>

  <dependencies>
    <dependency>
      <groupId>com.svenruppert.jsentinel</groupId>
      <artifactId>jSentinel-events</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>com.svenruppert.jsentinel</groupId>
      <artifactId>jSentinel-rest</artifactId>
      <version>${project.version}</version>
    </dependency>
    <!-- V00.75.10: typed fallible returns (Result) for the wire codec. -->
    <dependency>
      <groupId>com.svenruppert</groupId>
      <artifactId>functional-reactive</artifactId>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
