<?xml version="1.0" encoding="UTF-8"?>
<!--

    Copyright © 2018 Sven Ruppert (sven.ruppert@gmail.com)

    Licensed under the EUPL, Version 1.2 or - as soon they will be
    approved by the European Commission - subsequent versions of the
    EUPL (the "Licence"); You may not use this work except in
    compliance with the Licence. You may obtain a copy of the Licence at:

    https://joinup.ec.europa.eu/software/page/eupl

-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <parent>
    <groupId>eu.jsentinel</groupId>
    <artifactId>jCustos-parent</artifactId>
    <version>00.81.10-SNAPSHOT</version>
  </parent>
  <modelVersion>4.0.0</modelVersion>

  <artifactId>jCustos-events-persistence-eclipsestore</artifactId>
  <name>jCustos Events — Eclipse-Store persistence</name>
  <description>
    V00.75 Eclipse-Store-backed implementations of the four
    persistence SPIs in jCustos-events (EventStore, DeadLetterStore,
    ReplayStore, SequenceStore) plus the optional Eclipse Serializer
    PayloadCodec. Shares the V00.74.20 JCustosStoragePair lifecycle:
    consumers feed pair.app() into this module's stores so the
    Security-Event-Bus persistence rides the framework's two-phase
    shutdown.
  </description>

  <dependencies>
    <dependency>
      <groupId>eu.jsentinel</groupId>
      <artifactId>jCustos-events</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>eu.jsentinel</groupId>
      <artifactId>jCustos-persistence-eclipsestore</artifactId>
      <version>${project.version}</version>
    </dependency>

    <dependency>
      <groupId>eu.jsentinel</groupId>
      <artifactId>jCustos-events-testkit</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
