<!--
  jCustos persistence layer — Eclipse-Store backend.
  Adds ONE dependency. Eclipse-Store transitives come from the parent.
-->
<dependency>
    <groupId>eu.jsentinel</groupId>
    <artifactId>jCustos-persistence-eclipsestore</artifactId>
    <version>${jcustos.version}</version>
</dependency>
